# Standardized Definition Claim Structure

**Status:** Consolidated architectural extension v0.2.0  
**Parent standards:**
- `docs/architecture/relational-definition-standard.md`
- `docs/architecture/universal-claim-card-node-standard.md`
- `docs/architecture/definition-claim-nesting-and-composition.md`

## 1. Governing rule

Every protocol-relevant conception MUST be represented by a standardized Definition Claim-card.

A Definition Claim-card is not a second object type beside Claim. It is the canonical rendering of a rooted, recursively compositional Claim graph whose principal function is to make a conception identifiable, bounded, relationally navigable, operationally usable, contestable, and revisable.

All Definition Claim-cards use one standard structural grammar. They do not all instantiate the same quantity of local structure.

```text
StandardDefinitionGrammar = shared structure for defining conceptions
DefinitionStructure(c) = material fields, nested Claims, and relations generated by conception c
DefinitionStructure(c) ⊆ StandardDefinitionGrammar ⊆ UniversalClaimStructure
```

The standard combines:

1. a mandatory definition kernel required for every registered conception;
2. the full materially generated nested Claim graph of that conception; and
3. conditional modules instantiated whenever they are material to that conception and scope.

No empty placeholder is required for a module that is not material. No materially constitutive Claim may be omitted merely because the root definition sentence appears readable without it.

## 2. What requires a Definition Claim-card

A conception requires a registered Definition Claim-card when it materially controls any of the following:

- protocol meaning;
- graph interpretation;
- schema or validation behavior;
- governance, authority, or permission;
- workflow or process routing;
- identity, attribution, or responsibility;
- evidence or provenance interpretation;
- multilingual or glyph realization;
- legal, institutional, economic, social, or technical consequence;
- distinction among competing or conflicting Claims;
- any recurring term whose ambiguity can alter a material result.

Ordinary words do not need to be exhaustively pre-registered merely because they occur in language. They become definition candidates when their use acquires material architectural, operational, evidentiary, or governance significance.

## 3. Mandatory definition kernel

Every registered Definition Claim-card MUST possess the following kernel.

| Field | Requirement |
|---|---|
| `claim_id` | Stable, globally unique root Claim identifier. |
| `version` | Non-erasing version identifier. |
| `canonical_conception` | The conception being defined, independent of any one language label. |
| `preferred_label` | Current display label with language and script declared. |
| `exact_definition_claim` | Concise root Claim stating the current synthesis. |
| `nested_claim_graph` | Full materially generated recursive Claim composition needed to reconstruct, distinguish, apply, contest, validate, and revise the definition. |
| `declared_scope` | Domain, purpose, jurisdiction, scale, time, and audience for which the definition is asserted. |
| `relational_configuration` | What the conception distinguishes and how it relates included loci. |
| `inclusions` | What falls within the conception under the declared scope. |
| `exclusions` | What is expressly outside it where exclusion is material. |
| `boundary_conditions` | Tests, edge cases, thresholds, or conditions determining application. |
| `principal_relations` | Typed links to Claim-cards outside the nested composition that are most necessary to understand the conception. |
| `provenance` | Claiming loci, source manifestations, contribution history, and transformation record. |
| `epistemic_status` | Proposed, source-bound, contested, consolidated, ratified, superseded, or another declared state. |
| `governance_state` | Authority, ratification scope, permissions, and applicable review process. |
| `completeness_claim` | Explicit statement of represented nesting closure, imported dependencies, omissions, and unresolved Claims. |

A Definition Claim-card lacking any kernel field is not registrable as a canonical definition. Unknown information is represented as an unresolved Claim attached to the kernel; the field itself is not silently omitted.

The `exact_definition_claim` is the root of the card, not a substitute for its nested structure.

## 4. Nested Claim composition

The complete requirements for recursive composition are governed by `docs/architecture/definition-claim-nesting-and-composition.md`.

A Definition Claim-card MUST represent every materially relevant nested occurrence with:

- stable occurrence ID;
- independently addressable child Claim ID and version;
- parent occurrence;
- typed compositional role;
- nesting path;
- incorporation scope and state;
- provenance;
- qualifications, exceptions, and activation conditions where material.

A shared Claim is imported by typed, scoped occurrence rather than duplicated as a new Claim identity.

```text
ClaimIdentity(c)
≠ NestedOccurrence(c, parent, role, path, scope)
```

A relation is not automatically composition.

```text
RelatedTo(a,b)
≠ ConstitutiveOf(b,a)
```

The card MUST distinguish external relations from Claims that actually constitute, qualify, bound, evidence, operationalize, contest, or revise the root synthesis.

## 5. Conditional material modules

The following modules are instantiated whenever the conception materially generates them.

### 5.1 Linguistic module

- lexical forms;
- etymology;
- morphology;
- usage history;
- multilingual anchors;
- translation, analogy, partial-equivalence, and non-equivalence relations;
- protocol nomenclature;
- glyph or emoji-rider realization.

### 5.2 Formal module

- logical expression;
- mathematical realization;
- graph signature;
- ontology class or relation mapping;
- JSON Schema, RDF/OWL, SHACL, or runtime projection;
- invariants, arity, cardinality, precedence, and validation rules.

### 5.3 Operational module

- recognition procedure;
- application procedure;
- inputs and outputs;
- triggering conditions;
- process transitions;
- responsible loci;
- implementation and verification tests;
- failure and recovery conditions.

### 5.4 Evidentiary module

- supporting evidence;
- source quality;
- contrary evidence;
- uncertainty;
- prediction and resolution relations;
- observation conditions;
- evidence transformations and custody.

### 5.5 Historical and comparative module

- prior definitions;
- superseded definitions;
- competing frameworks;
- supporting, extending, conflicting, analogous, and paradoxical Claims;
- institutional and cultural histories;
- known conceptual drift.

### 5.6 Consequence and power module

- legal or governance effects;
- institutional enforcement;
- economic distribution;
- social or ecological consequences;
- authority asymmetries;
- coercive imposition;
- affected loci;
- repair and revision pathways.

### 5.7 Activation and manifestation module

- condition fields that activate the conception;
- perceptual, linguistic, computational, institutional, or behavioral manifestations;
- disposition and probability effects;
- dormant, active, competing, and inhibited configurations;
- relation to dialogic execution.

### 5.8 Semantic-dimension module

- distinctions made available by the definition;
- salience and valuation effects;
- interpretive continuations;
- neighboring and competing semantic dimensions;
- predictable conflations;
- effects of inactivation or absence;
- revision effects on interpretive geometry.

## 6. Canonical card template

```yaml
claim_id: C:<stable-root-id>
version: <non-erasing-version>
canonical_conception: <language-independent conception reference>
preferred_label:
  value: <label>
  language: <BCP-47 tag>
  script: <ISO 15924 tag where material>
exact_definition_claim: >
  <concise current root synthesis>

nested_claim_graph:
  root_claim_id: C:<stable-root-id>
  claim_occurrences:
    - occurrence_id: O:<stable-occurrence-id>
      claim_id: C:<child-claim>
      claim_version: <version>
      parent_occurrence_id: O:<parent-or-root-occurrence>
      role: <typed compositional role>
      path: /<nesting-path>
      incorporation_scope: <scope>
      incorporation_state: <adopted|contested|quoted|inherited|unresolved>
      necessity: <required|qualifying|illustrative|optional>
      activation_conditions: []
      local_qualifications: []
      provenance: []
  compositional_relations: []
  external_imports: []
  unresolved_claims: []
  closure_claim:
    condition_field: <Γt>
    represented_paths: []
    imported_dependencies: []
    unresolved_dependencies: []
    omitted_as_nonmaterial: []

declared_scope:
  domain: <scope>
  purpose: <scope>
  jurisdiction: <scope where material>
  temporal_condition: <scope>
  audience_or_participants: <scope>
relational_configuration:
  distinguishes: []
  relates: []
inclusions: []
exclusions: []
boundary_conditions: []
principal_relations:
  - relation: <typed external relation>
    target_claim_id: <Claim ID>
provenance:
  claiming_loci: []
  source_manifestations: []
  contribution_events: []
epistemic_status: <state>
governance_state:
  authority: []
  ratification_scope: <scope or none>
  review_process: <process>
completeness_claim:
  state: <captured|scope_structured|maximally_represented_at_condition|...>
  represented: []
  unresolved: []
  omitted_as_nonmaterial: []
material_modules:
  linguistic: {}
  formal: {}
  operational: {}
  evidentiary: {}
  historical_comparative: {}
  consequence_power: {}
  activation_manifestation: {}
  semantic_dimension: {}
```

The machine schema MUST permit absent conditional modules while requiring the kernel, nested Claim graph, and explicit unresolved Claims.

## 7. Relation and composition vocabulary

Definition Claim-cards SHOULD use external typed relations including:

- `DEFINES`;
- `DISTINGUISHES_FROM`;
- `INCLUDES`;
- `EXCLUDES`;
- `BOUNDED_BY`;
- `CONDITIONED_BY`;
- `MANIFESTS_AS`;
- `ACTIVATES`;
- `OCCASIONS`;
- `PRODUCES_SEMANTIC_RESULT`;
- `SUPPORTS`;
- `EXTENDS`;
- `CONFLICTS_WITH`;
- `CONTRASTS_WITH`;
- `ANALOGOUS_TO`;
- `PARTIALLY_EQUIVALENT_TO`;
- `TRANSLATED_AS`;
- `SUPERSEDES_NON_ERASING`;
- `OPERATIONALIZED_BY`;
- `VALIDATED_BY`;
- `GOVERNED_BY`;
- `CONTRIBUTED_BY`;
- `DERIVED_FROM`.

Nested composition SHOULD use roles including:

- `COMPOSES_FROM`;
- `CONSTITUTIVE_OF`;
- `SUBCLAIM_OF`;
- `DECOMPOSES`;
- `QUALIFIES`;
- `BOUNDS`;
- `CONDITIONS`;
- `INCLUDES_WITHIN`;
- `EXCLUDES_FROM`;
- `EXCEPTION_TO`;
- `ASSUMED_BY`;
- `SUPPORTED_BY`;
- `EVIDENCED_BY`;
- `DERIVED_FROM`;
- `IMPLIES`;
- `OPERATIONALIZES`;
- `VALIDATES`;
- `EXEMPLIFIES`;
- `CONFLICTS_WITH`;
- `COUNTERCLAIMS`;
- `ALTERNATIVE_TO`;
- `SUPERSEDES_NON_ERASING`;
- `UNRESOLVED_WITHIN`.

Relation and composition-role identifiers are themselves conceptions and require Definition Claim-cards.

## 8. Definition lifecycle

| State | Meaning |
|---|---|
| `identified` | A materially significant conception has been detected and assigned an identifier. |
| `captured` | Exact originating manifestation and initial provenance are preserved. |
| `kernel_drafted` | Every mandatory kernel field is represented, including unresolved values. |
| `nesting_drafted` | The root synthesis has an explicit nested Claim structure with occurrence identities and compositional roles. |
| `scope_structured` | The definition's material local structure and principal external relations have been identified. |
| `source_bound` | Material evidence, authorities, and source spans are attached. |
| `contested` | Competing or conflicting definitions remain active. |
| `consolidation_candidate` | A coherent current definition is proposed with boundaries and unresolved matters visible. |
| `consolidated` | Participating loci accept the definition as the current shared construction. |
| `ratified` | The exact version has declared governance force for a stated scope. |
| `implemented` | All material graph, schema, workflow, glyph, multilingual, nesting, and validation derivatives exist. |
| `maximally_represented_at_condition` | No presently available material Claim or structure within scope is omitted. |
| `superseded_non_erasing` | A later version is operative while this definition and its effects remain recoverable. |

## 9. Completeness and validation

A Definition Claim-card is complete at condition field `Γt` only when:

```text
DefinitionComplete(c, Γt)
:= KernelComplete(c)
 ∧ NestedClaimClosureExplicit(c, Γt)
 ∧ RootReconstructibleFromNesting(c)
 ∧ ScopeExplicit(c)
 ∧ BoundariesTestable(c)
 ∧ ExternalRelationsTyped(c)
 ∧ ProvenancePreserved(c)
 ∧ NoMaterialModuleOmitted(c, Γt)
 ∧ UnresolvedClaimsVisible(c)
```

Completion does not mean finality. Definitions remain revisable through non-erasing Claims.

Validation MUST reject:

- a root synthesis that cannot be reconstructed from its nested Claims;
- materially constitutive Claims represented only as untyped external links;
- nested Claims flattened into prose without stable Claim identities;
- shared Claims duplicated rather than reused through scoped occurrences;
- circular wording that supplies no independently testable distinction and fails to declare the cycle;
- silent conflation of neighboring conceptions;
- unnamed scope changes;
- untyped external or compositional relations;
- source Claims without provenance;
- governance force without declared authority and scope;
- an English label treated as the canonical ontology identity;
- hidden exceptions, conflicts, or unresolved dependencies;
- empty universal placeholders used to simulate completeness;
- definition changes that erase prior versions or acquired consequences.

## 10. Recursive definition and nesting rule

Definitions necessarily depend on other definitions. This does not require an impossible terminal definition for every word before use.

The graph resolves dependency recursively:

```text
Definition A
  → nested constitutive Claim B
  → imported Definition B closure
  → dependent Claim C
  → grounded through source, operation, contrast, example, or measurable consequence
```

A cycle is permitted when the cycle is real and explicitly represented. It MUST NOT masquerade as independent grounding.

The definition process advances through a growing, testable network rather than through a flat glossary.

## 11. Registry architecture

The canonical registry MUST maintain:

- one stable root Claim ID per conception;
- every definition version;
- stable nested-occurrence IDs and paths;
- labels and anchors without making labels the identity;
- compositional, dependency, and external relation edges;
- lifecycle state;
- unresolved work;
- implementation status;
- conflict and alternative definitions;
- provenance and contribution;
- machine-readable serialization;
- compact and full card rendering;
- task-relevant nested-closure retrieval.

The registry is a graph index over recursively compositional Definition Claim-cards, not a document containing final prose entries.

## 12. Initial build order

The first definition wave MUST establish the concepts used to define all later concepts:

1. Claim;
2. Claim identity;
3. Claim occurrence;
4. Claim nesting and composition;
5. conception;
6. definition;
7. locus;
8. relation;
9. composition role;
10. scope;
11. condition field;
12. manifestation;
13. distinction and difference;
14. inclusion, exclusion, and boundary;
15. semantics and meaning;
16. information and knowledge;
17. possibility and probability;
18. activation and disposition;
19. identity and continuity;
20. provenance and evidence;
21. governance, authority, consolidation, and ratification;
22. graph, node, edge, card, event, state, and process;
23. dislocation, acquired difference, reconnection, and union;
24. language, dialogue, language-game, and fractal language-game;
25. Lux, execution locus, canonical state, checkpoint, and handoff.

Each completed wave exposes the material dependency set for the next wave.

## 13. Constitutional formulation

> Caeluviim defines conceptions through one standardized, recursively compositional Definition Claim structure. The root definition Claim compresses the current synthesis, while the nested Claim graph preserves every materially constitutive, qualifying, bounding, evidentiary, operational, conflicting, historical, and unresolved Claim required to reconstruct and use the conception. Nested Claims retain independent identity and provenance; their participation is represented through typed, scoped occurrences. The definition registry is therefore a recursively connected, provenance-bearing Claim graph rather than a flat glossary, field list, or collection of summary sentences.
